stages:
  - build

variables:
  GIT_SUBMODULE_STRATEGY: recursive

build:
  stage: build
  image: node:13
  script:
    - npm install
    - ./node_modules/.bin/ng build
    - tar cJf dsc.tar.xz -C dist/DSC/ .
  artifacts:
    paths:
      - ./dsc.tar.xz
